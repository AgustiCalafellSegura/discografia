{% extends "base.html.twig" %}

{% block title %}
    Nom de la cançó
{% endblock %}

{% block body %}
    <h1>Song List</h1>

    {% if song is not null %}
        <table>
            <thead>
            <tr>
                <th>id</th>
                <th>name</th>
                <th>duration</th>
                <th>stars</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for song in songs %}
                <ul>
                    {% for song in songs %}
                        <li>{{ song.name }} <a href="{{ path('app_lucky_song', {id: song.id}) }}">Cançó {{ song.id }}</a></li>
                    {% endfor %}
                </ul>
                <tr>
                    <td>{{ song.id }}</td>
                    <td>{{ song.name }}</td>
                    <td>{{ song.duration }}</td>
                    <td>{{ song.stars }}</td>
                    <td></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    {% else %}
        <p>No hi ha dades per mostrar</p>

    {% endif %}
        <a href="{{ path('app_lucky_songslist') }}">Botó</a>

{% endblock %}